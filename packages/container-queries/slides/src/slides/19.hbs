<section>
  <h2 class="mb-2">TIL</h2>
  <div class="mb-2">
    <strong>Container Queries</strong>
    <p class="mb-3">Allows any element with both size & layout containment to be queried using a new <code>@container</code> rule, with similar syntax to existing media-queries.</p>
    <pre class="mb-3"><code data-trim class="hlcss disable-line-numbers">
    .container {
      contain: layout inline-size style;
    }

    @container (min-width: 100px) {
      .child-of-container { /* ... */ }
    }
    </code></pre>
    <p><strong>Draft</strong> <br>Experiment on Chrome Canary with <code>chrome://flags/#enable-container-queries</code> enabled</p>
  </div>

</section>