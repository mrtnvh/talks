<section>
  <h2 class="mb-2">TIL</h2>
  <div class="mb-2">

    <p class="mb-2"><strong>Container Queries</strong>: Allows any element with both size & layout containment to be queried using a new <code>@container</code> rule, with similar syntax to existing media-queries.</p>
    <div>
      <div class="grid grid--two-columns">
        <div><pre><code data-trim class="hlcss disable-line-numbers">
        .container {
          contain: layout inline-size style;
        }
        </code></pre></div>
        <div><pre><code data-trim class="hlcss disable-line-numbers">
        @container (min-width: 100px) {
          .child-of-container { /* ... */ }
        }
        </code></pre></div>
      </div>
    </div>
  </div>

</section>